paths:
  /api/classes:
    get:
      tags:
        - Classes
      summary: "Get classes"
      description: Get classes the database.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/classes"
      responses:
        "200":
          description: Get classes the database.
        "422":
          description: Could not fetch data from database.
