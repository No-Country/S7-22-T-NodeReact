paths:
  /api/career:
    get:
      tags:
        - Careers
      summary: "Get careers"
      description: Get careers the database.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/careers"
      responses:
        "200":
          description: Get careers the database.
        "422":
          description: Could not fetch data from database.
